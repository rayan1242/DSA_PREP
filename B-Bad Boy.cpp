#include <iostream>
#include <cmath>
#include <vector>
using namespace std;

void solve() {
    int n, m, x, y;
    cin >> n >> m >> x >> y;
	cout<<1<<" "<<1<<" "<<n<<" "<<m<<"\n";
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int T;
    cin >> T;

    while (T--) {
        solve();
    }

    return 0;
}

